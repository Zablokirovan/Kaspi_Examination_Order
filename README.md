**Kaspi Examination Order**

Сервис предназначен для автоматического обновления стадий сделок в Bitrix24 на основании информации о заказах из Kaspi.
Интеграция от ITL не всегда работает корректно, поэтому данный сервис выполняет дополнительную проверку статусов заказов и синхронизирует стадии сделок напрямую.


**📦 Клонирование репозитория**
```
git clone https://github.com/Zablokirovan/Kaspi_Examination_Order.git
cd Kaspi_Examination_Order
```

**⚙️ Настройка окружения**
Создайте файл .env на основе примера:
**Linux / macOS / WSL**
```
cp .env_example .env
```
**Windows (PowerShell)**
```
Copy-Item .env_example .env
```

**После создания файла .env укажите в нём необходимые параметры:**
```
KASPI_TOKEN
BITRIX_WEBHOOK
TELEGRAM_TOKEN
```
параметры задержек (при необходимости)

**🐳 Сборка Docker-контейнера**
```
docker compose build
```
**▶️ Запуск сервиса**
```
docker compose up
````

**Для запуска в фоновом режиме:**
```
docker compose up -d
```

**📋 Просмотр логов**
```
docker compose logs -f
```

**🔄 Как работает сервис**
*Контейнер запускается и начинает бесконечный цикл обработки.
*Проходит по всем заданным стадиям воронки.
*Получает сделки из Bitrix24.
*Проверяет статус заказов в Kaspi.
*Обновляет стадии сделок при необходимости.
*Делает паузы между стадиями и полными циклами для снижения нагрузки.

**🛑 Остановка сервиса**
```
docker compose down
```
